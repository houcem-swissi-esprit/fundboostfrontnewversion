import { FundingTransaction } from './funding-transaction';

describe('FundingTransaction', () => {
  it('should create an instance', () => {
    expect(new FundingTransaction()).toBeTruthy();
  });
});
