import { NotificationRequest } from './notification-request';

describe('NotificationRequest', () => {
  it('should create an instance', () => {
    expect(new NotificationRequest()).toBeTruthy();
  });
});
